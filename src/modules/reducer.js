import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const prcSlice = createSlice({
  name: "prac",
  reducers: {},
});

export const {} = prcSlice.actions;
export default prcSlice.reducer;
